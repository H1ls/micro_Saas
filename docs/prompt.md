Изучи перед началом работы:
1. `docs/mvp_ai_dashboard_architecture_48h.md`
2. `docs/execution_plan.md`
3. `docs/ai_execution_rules.md`
4. `docs/execution_status.md`
Реализуй **Block N** из `docs/execution_plan.md`.

Перед началом:
* проверь статус Block N в `execution_status.md`;
* убедись, что зависимости Block N завершены;
* изучи только существующие файлы и зависимости, которые напрямую относятся к Block N;
* не перечитывай весь проект без необходимости;
* не создавай дубли существующей логики.

Используй `execution_plan.md` и архитектуру, чтобы определить минимальный набор файлов, который нужно проверить перед изменениями.

Строго соблюдай:

* архитектуру из `mvp_ai_dashboard_architecture_48h.md`;
* scope текущего блока;
* правила из `ai_execution_rules.md`;
* критерии готовности из `execution_plan.md`.

Не переходи к Block N+1 и последующим блокам.

После реализации:

* самостоятельно проверь результат;
* выполни предусмотренные тесты;
* обнови `execution_status.md`;
* создай commit по Git-правилам проекта;
* покажи краткий отчёт;
* перечисли архитектурные отклонения, если они были.
* Отдельно дай мне пошаговую инструкцию, как самостоятельно проверить,если для проверки нужен файл, приведи минимальный пример его содержимого.
Если отклонений нет, напиши:
`Отклонений от утвержденной архитектуры нет.`
После этого остановись.
